### ***
### WARNING: DO NOT manually EDIT or MERGE this file, it is generated by 'make ci-config'.
### INSTEAD: Edit or merge the source in config/ then run 'make ci-config'.
### ***
version: 2
jobs:
  test:
    docker:
    - image: cimg/base:2020.01
    environment:
    - DEFAULT_BRANCH: master
    shell: /usr/bin/env bash -euo pipefail
    steps:
    - checkout
    - attach_workspace:
        at: .
    - run:
        command: mkdir master
    - run:
        command: echo $DEFAULT_BRANCH
    - run:
        command: echo $BRANCH
    - persist_to_workspace:
        paths:
        - master
        root: .
workflows:
  ci:
    jobs:
    - test:
        filters:
          branches:
            ignore: master
  version: 2

# Original config.yml file:
# executors:
#   go:
#     docker:
#     - image: golang:1.14.6
#     environment:
#       BRANCH: master
#       GOPATH: /go
#     working_directory: /go/src/github.com/mdeggies/circleci-tester
# jobs:
#   test:
#     docker:
#     - image: cimg/base:2020.01
#     environment:
#       DEFAULT_BRANCH: master
#     shell: /usr/bin/env bash -euo pipefail
#     steps:
#     - checkout
#     - attach_workspace:
#         at: .
#     - run: mkdir master
#     - run: echo $DEFAULT_BRANCH
#     - run: echo $BRANCH
#     - persist_to_workspace:
#         paths:
#         - master
#         root: .
# references:
#   common_envs:
#     BRANCH: master
#   go-machine-image: circleci/classic:201808-01
# version: 2.1
# workflows:
#   ci:
#     jobs:
#     - test:
#         filters:
#           branches:
#             ignore: master